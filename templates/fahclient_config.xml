<config>
  <!-- User Information -->
  <passkey v='{{ fah_config['passkey']'/>
  <team v='{{ fah_config['team'] }}'/>
  <user v='{{ fah_config['user'] }}'/>

  <!-- Folding Slots -->
  <slot id='0' type='GPU'>
    <pci-bus v='1'/>
    <pci-slot v='0'/>
  </slot>
</config>
